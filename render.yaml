services:
  - type: web
    name: google-maps-restaurant-list-rag
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn src.app:app
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.12
